# AVR-camera
A charlieplexing based approach paired with 12 LDR sensors to click black and white photo and convert them into bmp files using python and C language. Also uses custom cubic interpolation to upscale image.

(port to baremetal coming soon)

library requried for python -> pyserial, numpy, pillow
